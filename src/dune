;; installed executables / public targets
(executables
  (names daft daft_mds daft_ds)
  (public_names daft daft_mds daft_ds)
  (modules utils types socket_wrapper meta_data_server data_server colors client)
  (libraries dolog batteries zmq cryptokit base-unix))
